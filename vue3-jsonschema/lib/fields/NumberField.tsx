import { defineComponent, PropType } from "vue";

export default defineComponent({
    name: 'NumberField',
    setup () {
        return () => {
            return (<div>this is number fields</div>);
        };
    }
});