import { defineComponent, PropType } from "vue";

export default defineComponent({
    name: 'StringField',
    setup () {
        return () => {
            return (<div>this is string fields</div>);
        };
    }
});